{{> partials/navbar }}
<div class="container vh-100" id="main">
   {{!-- <div class="containerButton">
            <button type="button" class="buttonAgregarTarea"><i class="bi bi-plus-circle"></i> Nueva tarea</button>
   </div>
    <h3><i class="bi bi-arrow-down-circle"></i>{{> partials/title }}</h3>  --}}
    {{!-- <div class="contentTable">
            <table class="table table-striped table-hover">
                <thead>
                  <tr>
                    <th scope="col" class="whiteSpace">Título de la tarea</th>
                    <th scope="col">Descripción</th>
                    <th scope="col">Acciones</th>
                  </tr>
                </thead>
                <tbody id="printTareas">
                  
                </tbody>
              </table>
    </div> --}}


    <div class="container mt-5">
      <form action="/enviar" id="formularioNombre">
        <div class="mb-3">
          <input type="text" class="form-control" id="nombre" name="nombre">
        </div>
        <div class="mb-3">
          <input type="text" class="form-control" id="apellido" name="apellido">
        </div>
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </div>
</div>
{{> partials/footer}}